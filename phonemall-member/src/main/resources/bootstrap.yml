spring:
  application:
    name: phonemall-member
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        namespace: phonemall-member
        group: DEFAULT_GROUP
        name: application
