spring:
  application:
    name: phonemall-cart
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        namespace: phonemall-cart
        group: DEFAULT_GROUP
        name: application
