spring:
  application:
    name: phonemall-auth-server
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        namespace: phonemall-auth-server
        group: DEFAULT_GROUP
        name: application
