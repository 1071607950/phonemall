spring:
  application:
    name: phonemall-product
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        namespace: phonemall-product
        group: DEFAULT_GROUP
        name: application
