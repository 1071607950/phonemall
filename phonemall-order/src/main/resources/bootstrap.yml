spring:
  application:
    name: phonemall-order
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        namespace: phonemall-order
        group: DEFAULT_GROUP
        name: application
